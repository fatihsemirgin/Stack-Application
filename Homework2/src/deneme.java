
public class deneme {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Stack deneme = new Stack(2);
		
		deneme.push(1);
		int a = (int) deneme.pop();
		System.out.println(a);
		
		/*
		Stack index = new Stack(15);
		int ix=0;
		File dosya = new File("D://fruits.txt");
		BufferedReader inputStream = null;
        try {
        	inputStream = new BufferedReader(
        			new InputStreamReader(
        			new FileInputStream(dosya), "UTF8"));                 
            String satir;
            while ((satir = inputStream.readLine()) != null) {
            	fruitStack.push(satir);
            	index.push(ix);
            	//System.out.println(fruitStack.peek());
            	//System.out.println(index.peek());
            	ix++;
            }
        }
        finally {
            if (inputStream != null) {
                inputStream.close();
            }
        }
        */
		
		
		/*while(!scores.ýsEmpty()) {
		scores_temp.push(scores.pop());
	}*/
		
		//ö=point;
		
		/*
		if(flag==true) {
			//System.out.println(stack1.peek());
			//System.out.println("Size  "+stack1.size());
		}
		else
			//System.out.println("Random sayý ayný , eleman eklenemedi!!!");
		*/
	}

}
